apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: hriv-backend
  namespace: hriv
spec:
  interval: 5m
  chart:
    spec:
      chart: hriv-backend
      # Pinned to stable releases only — bump manually or via promotion PR.
      version: ">=1.0.0"
      sourceRef:
        kind: HelmRepository
        name: hriv
  driftDetection:
    mode: enabled
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
      remediateLastFailure: true
  values:
    oidc:
      enabled: true
      redirectUri: "https://hriv.ltc.bcit.ca/api/auth/oidc/callback"
      postLoginRedirect: "https://hriv.ltc.bcit.ca"
      corsOrigins: "https://hriv.ltc.bcit.ca"
    vault:
      enabled: true
