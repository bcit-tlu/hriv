apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: hriv-frontend
  namespace: hriv
spec:
  interval: 5m
  chart:
    spec:
      chart: hriv-frontend
      # Pinned to stable releases only — bump manually or via promotion PR.
      version: ">=1.0.0"
      sourceRef:
        kind: HelmRepository
        name: hriv
  driftDetection:
    mode: enabled
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
      remediateLastFailure: true
  values:
    ingress:
      enabled: true
      className: nginx
      annotations:
        nginx.ingress.kubernetes.io/proxy-body-size: "0"
      hosts:
        - host: hriv.ltc.bcit.ca
          paths:
            - path: /
              pathType: Prefix
      tls:
        - hosts:
            - hriv.ltc.bcit.ca
          secretName: hriv-stable-tls
