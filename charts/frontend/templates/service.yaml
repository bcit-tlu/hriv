apiVersion: v1
kind: Service
metadata:
  name: {{ include "hriv-frontend.fullname" . }}
  labels:
    {{- include "hriv-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hriv-frontend.selectorLabels" . | nindent 4 }}
