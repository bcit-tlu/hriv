{{/*
The backup chart intentionally does not render credentials. azureSecretName must
identify a pre-existing Secret populated by an operator or external secret
controller with key AZURE_STORAGE_CONNECTION_STRING.
*/}}
