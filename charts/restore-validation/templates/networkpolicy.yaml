apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: hriv-restore-validation-default-deny
  labels:
    {{- include "hriv-restore-validation.labels" . | nindent 4 }}
spec:
  podSelector: {}
  policyTypes: [Ingress, Egress]
