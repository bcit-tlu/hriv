{{- if .Values.vault.enabled }}
# VSO static secret for the CNPG app user credentials.
# Vault path should contain keys: username, password
# VSO renders these into a kubernetes.io/basic-auth Secret that CNPG
# references as the bootstrap owner credential.
apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultStaticSecret
metadata:
  name: {{ include "hriv-backend.fullname" . }}-db-bootstrap
  labels:
    {{- include "hriv-backend.labels" . | nindent 4 }}
spec:
  type: kv-v2
  mount: {{ .Values.vault.kvMount }}
  path: {{ .Values.vault.dbAppSecretPath }}
  vaultAuthRef: {{ .Values.vault.authRef }}
  refreshAfter: {{ .Values.vault.refreshAfter }}
  destination:
    name: {{ include "hriv-backend.fullname" . }}-db-bootstrap
    create: true
    type: kubernetes.io/basic-auth
---
# VSO static secret for the CNPG superuser credentials.
# Vault path should contain keys: username, password
apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultStaticSecret
metadata:
  name: {{ include "hriv-backend.fullname" . }}-db-superuser-bootstrap
  labels:
    {{- include "hriv-backend.labels" . | nindent 4 }}
spec:
  type: kv-v2
  mount: {{ .Values.vault.kvMount }}
  path: {{ .Values.vault.dbSuperuserSecretPath }}
  vaultAuthRef: {{ .Values.vault.authRef }}
  refreshAfter: {{ .Values.vault.refreshAfter }}
  destination:
    name: {{ include "hriv-backend.fullname" . }}-db-superuser-bootstrap
    create: true
    type: kubernetes.io/basic-auth
---
# VSO static secret that builds the application connection URI expected by
# the backend Deployment (DATABASE_URL).
# CNPG does NOT auto-generate the <cluster>-app secret when
# bootstrap.initdb.secret.name is provided, so we construct the
# postgresql:// URI here from the same Vault username/password.
apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultStaticSecret
metadata:
  name: {{ include "hriv-backend.fullname" . }}-db-app
  labels:
    {{- include "hriv-backend.labels" . | nindent 4 }}
spec:
  type: kv-v2
  mount: {{ .Values.vault.kvMount }}
  path: {{ .Values.vault.dbAppSecretPath }}
  vaultAuthRef: {{ .Values.vault.authRef }}
  refreshAfter: {{ .Values.vault.refreshAfter }}
  destination:
    name: {{ include "hriv-backend.fullname" . }}-db-app
    create: true
    type: Opaque
    transformation:
      excludes:
        - "*"
      templates:
        uri:
          text: {{ printf `postgresql://{{ get .Secrets "username" | urlquery }}:{{ get .Secrets "password" | urlquery }}@%s-db-rw:5432/%s` (include "hriv-backend.fullname" .) .Values.cnpg.database | quote }}
---
# VSO static secret for the GitHub PAT used by issue reporting.
# Vault path should contain key: token
apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultStaticSecret
metadata:
  name: {{ include "hriv-backend.fullname" . }}-report-issue-pat
  labels:
    {{- include "hriv-backend.labels" . | nindent 4 }}
spec:
  type: kv-v2
  mount: {{ .Values.vault.kvMount }}
  path: {{ .Values.vault.reportIssuePatSecretPath }}
  vaultAuthRef: {{ .Values.vault.authRef }}
  refreshAfter: {{ .Values.vault.refreshAfter }}
  destination:
    name: {{ .Values.reportIssue.existingSecret | default (printf "%s-report-issue-pat" (include "hriv-backend.fullname" .)) }}
    create: true
    type: Opaque
---
# VSO static secret for OIDC client credentials.
# Vault path should contain keys: client-id, client-secret
{{- if $.Values.oidc.enabled }}
apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultStaticSecret
metadata:
  name: {{ include "hriv-backend.fullname" . }}-oidc
  labels:
    {{- include "hriv-backend.labels" . | nindent 4 }}
spec:
  type: kv-v2
  mount: {{ .Values.vault.kvMount }}
  path: {{ .Values.vault.oidcSecretPath }}
  vaultAuthRef: {{ .Values.vault.authRef }}
  refreshAfter: {{ .Values.vault.refreshAfter }}
  destination:
    name: {{ include "hriv-backend.fullname" . }}-oidc
    create: true
    type: Opaque
{{- end }}
{{- end }}
