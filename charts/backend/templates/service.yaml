apiVersion: v1
kind: Service
metadata:
  name: {{ include "corgi-backend.fullname" . }}
  labels:
    {{- include "corgi-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "corgi-backend.selectorLabels" . | nindent 4 }}
