apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress
spec:
  tls:
    - hosts:
        - latest--corgi.ltc.bcit.ca
      secretName: star-ltc-bcit-ca
  rules:
    - host: latest--corgi.ltc.bcit.ca
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: corgi-nginx-service
                port:
                  number: 80
