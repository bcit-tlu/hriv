apiVersion: v1
kind: Service
metadata:
  name: {{ include "corgi.fullname" . }}-frontend
  labels:
    {{- include "corgi.labels" . | nindent 4 }}
    {{- include "corgi.frontend.selectorLabels" . | nindent 4 }}
spec:
  type: {{ .Values.frontend.service.type }}
  ports:
    - port: {{ .Values.frontend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "corgi.frontend.selectorLabels" . | nindent 4 }}
